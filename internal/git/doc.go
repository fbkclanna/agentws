// Package git provides a wrapper around Git CLI commands used by agentws.
// It handles clone, fetch, checkout, branch operations, and dirty-state
// detection without depending on other internal packages.
package git
