// Package manifest handles parsing and validation of workspace.yaml files.
// It provides the data model for workspace configuration including repos,
// profiles, defaults, and filtering logic.
package manifest
